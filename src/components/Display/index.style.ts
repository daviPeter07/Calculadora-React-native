import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  displayContainer: {
    flex: 1,
    justifyContent: "flex-end",
    alignItems: "flex-end",
    paddingVertical: 24,
    paddingHorizontal: 4,
    minHeight: 120,
  },
  display: {
    fontSize: 64,
    fontWeight: "300",
  },
});
