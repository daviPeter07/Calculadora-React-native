import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    flexDirection: "row",
    alignItems: "center",
    paddingVertical: 12,
    minHeight: 48,
  },
  historyButton: {
    padding: 8,
  },
  pressed: {
    opacity: 0.6,
  },
});
